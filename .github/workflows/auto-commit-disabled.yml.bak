# DISABLED - This workflow was causing repeated failures
# Original auto-commit workflow moved here as backup
# Use manual-sync.yml instead for safe manual triggers

name: Automated Git Commits and Sync (DISABLED)

# Disabled all triggers to stop failures
# on:
#   schedule:
#     - cron: '0 */6 * * *'
#   workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
    - name: Workflow Disabled
      run: |
        echo "This workflow has been disabled to prevent repeated failures"
        echo "Use manual-sync.yml for manual synchronization"
        echo "Or re-enable by uncommenting the 'on:' triggers above"